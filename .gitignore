build/
# CMake build trees (out-of-source) + in-source config artifacts
build-cmake/
build-rel/
CMakeCache.txt
CMakeFiles/
*.o
*.a
*.gcda
*.gcno
*.profraw
*.profdata
*.plist
*.dSYM/
.DS_Store

# UI build + deps (committed: ui/src; ignored: outputs/deps)
ui/node_modules/
ui/dist/

# webview is cloned by scripts/fetch_webview.sh (cJSON under src/vendor IS committed)
src/vendor/webview/
# iroh-c-ffi is cloned by scripts/fetch_iroh.sh (the lone Rust dep; share/app builds only)
src/vendor/iroh-c-ffi/
# WebView2 SDK headers (Microsoft.Web.WebView2 nuget) — fetched, not committed (Windows GUI)
src/vendor/webview2/

# user data — a book is a private SQLite file; NEVER publish real financial data
*.sqlite
*.sqlite-shm
*.sqlite-wal
books.json

# local editor / tooling config
.claude/
